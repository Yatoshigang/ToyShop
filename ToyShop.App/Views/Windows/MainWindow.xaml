<Window x:Class="ToyShop.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToyShop.App.Views"
        mc:Ignorable="d"
        Title="{Binding }" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height=".15*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width=".2*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button Content="Профиль" Command="{Binding }"/>
                <Button Content="Категории" Command="{Binding }"/>
                <Button Content="Бренд" Command="{Binding }"/>
                <Button Content="Товары" Command="{Binding }"/>
            </StackPanel>
            <Button Grid.Column="1" Content="Выход" Command="{Binding }"/>
        </Grid>
    </Grid>
</Window>
